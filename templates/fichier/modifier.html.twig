{% extends 'base.html.twig' %}

{% block title %}Modification du fichier{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_label(form.nom, 'Nom:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.nom) }}
            {{ form_widget(form.nom) }}
            {{ form_help(form.nom) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.nomoriginal, 'Nom Original:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.nomoriginal) }}
            {{ form_widget(form.nomoriginal) }}
            {{ form_help(form.nomoriginal) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.date, 'Date : ', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.date) }}
            {{ form_widget(form.date) }}
            {{ form_help(form.date) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.extension, 'Extension:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.extension) }}
            {{ form_widget(form.extension) }}
            {{ form_help(form.extension) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(form.taille, 'Taille:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.taille) }}
            {{ form_widget(form.taille) }}
            {{ form_help(form.taille) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.user, 'Utilisateur:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.user) }}
            {{ form_widget(form.user) }}
            {{ form_help(form.user) }}
        </div>
    </div>
        <div class="form-group">
        {{ form_label(form.themes, 'Thème:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_errors(form.themes) }}
            {{ form_widget(form.themes) }}
            {{ form_help(form.themes) }}
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            {{ form_widget(form.save) }}
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
